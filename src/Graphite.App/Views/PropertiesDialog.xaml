<Window x:Class="Graphite.App.Views.PropertiesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Document properties"
        WindowStartupLocation="CenterOwner" SizeToContent="Height" Width="460"
        WindowStyle="ToolWindow" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource App.BackgroundBrush}"
        TextElement.Foreground="{DynamicResource App.TextBrush}"
        FontFamily="Segoe UI Variable, Segoe UI" FontSize="12.5">
    <StackPanel Margin="20">
        <TextBlock x:Name="FileName" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,12"
                   TextTrimming="CharacterEllipsis"/>
        <ItemsControl x:Name="Rows">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,7">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Item1}" Style="{StaticResource Caption}" FontSize="12"/>
                        <TextBlock Grid.Column="1" Text="{Binding Item2}" Style="{StaticResource Body}"
                                   TextWrapping="Wrap" FontSize="12"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Close" Style="{StaticResource AccentButton}" IsCancel="True" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
